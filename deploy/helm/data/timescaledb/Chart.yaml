apiVersion: v2
name: cex-timescaledb
description: TimescaleDB chart for CEX data plane (wrapper)
type: application
version: 0.1.0
appVersion: "2"

dependencies:
  - name: timescaledb-single
    version: "0.33.1"
    repository: "https://charts.timescale.com"
